import './env.js';

const main = async (): Promise<void> => {
  console.log('IN DEV');

  // fetch the show from plex
  // fetch the show from anidb
  // iterate through each episode and apply the updates
};

main();
